set(BENCH_LIB_PATH ${SUPPORT_PATH}/${BENCH_SUPPORT_LIB_DIR}) 
set(OPENCL_WRAPPER_PATH ${SUPPORT_PATH}/${OPENCL_WRAPPER_DIR})

add_subdirectory(${BENCH_LIB_PATH})
add_subdirectory(${OPENCL_WRAPPER_PATH})
